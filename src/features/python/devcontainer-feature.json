{
    "name": "Python",
    "id": "python",
    "version": "0.0.1",
    "description": "Installs uv, and optionally a Python version managed by it",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "3.15",
                "3.15t",
                "3.14",
                "3.14t",
                "3.13",
                "3.12",
                "3.11"
            ],
            "default": "",
            "description": "Python version for uv to install. Leave blank to install uv only. Anything `uv python install` accepts works, including free-threaded builds (3.14t) and PyPy (pypy@3.11)."
        }
    },
    // sync the workspace's environment if the project has a manifest
    "postAttachCommand": "(test -f pyproject.toml && uv sync) || true"
}
